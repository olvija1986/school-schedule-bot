#name: keepalive

#on:
  #schedule:
    #- cron: "*/5 * * * *"
  #workflow_dispatch: {}

#jobs:
  #ping:
    #runs-on: ubuntu-latest
    #steps:
      #- name: Ping Render app
        #run: |
          #curl -fsS "https://school-schedule-bot.onrender.com/" >/dev/null
